# ⚠️  CRITICAL: SSH Security Configuration
# This .gitignore MUST protect sensitive SSH information

# === NEVER COMMIT THESE FILES ===
# Main SSH config (contains real IPs, usernames, hosts)
config

# === SSH Keys (ALL formats) ===
*.key
*.pem
*_rsa
*_rsa.pub
*_ed25519
*_ed25519.pub
*_ecdsa
*_ecdsa.pub
*_dsa
*_dsa.pub
id_*

# === SSH Runtime Files ===
known_hosts*
authorized_keys*
.ssh_auth_sock

# === SSH Control Sockets ===
sockets/
*.sock

# === SSH Agent Files ===
agent.*
ssh-agent.*

# === Temporary/Backup Files ===
*.bak
*.tmp
*.old
config.backup
config.local

# === SAFE TO COMMIT ===
!config.template
!config.example
!.gitignore
!README.md

# === Additional Security ===
# Any file containing 'secret', 'private', 'key' in name
*secret*
*private*
*password*
*credential*

# === Log files ===
*.log
ssh_debug.log